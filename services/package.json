{
  "name": "@compliance-shepherd/services",
  "version": "1.0.0",
  "description": "Lambda services for AI Compliance Shepherd",
  "private": true,
  "workspaces": [
    "scan-environment",
    "analyze-terraform-plan",
    "apply-fix",
    "generate-audit-pack",
    "api-gateway"
  ],
  "scripts": {
    "build": "npm run build --workspaces --if-present",
    "test": "npm run test --workspaces --if-present",
    "lint": "eslint . --ext .ts,.js",
    "format": "prettier --write ."
  },
  "devDependencies": {
    "@types/aws-lambda": "^8.10.0",
    "@types/node": "^20.0.0",
    "typescript": "^5.0.0",
    "eslint": "^8.0.0",
    "prettier": "^3.0.0"
  }
}
